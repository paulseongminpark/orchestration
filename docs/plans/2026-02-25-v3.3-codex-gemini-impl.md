# v3.3 Codex/Gemini CLI 통합 강화 — 구현 플랜

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Codex/Gemini CLI를 컨텍스트 오프로딩 + tech-review QA + 병렬 코드 리뷰에 활용하는 v3.3 시스템 구축

**Architecture:** Gemini = 벌크 추출기 (1M 컨텍스트, 스킬 기반), Codex = 정밀 검증기 (프로필 기반), Claude = 최종 판단자 (verify barrier). 모든 외부 CLI 출력은 JSON + _meta 블록으로 구조화.

**Tech Stack:** Codex CLI v0.104.0 (gpt-5.3-codex), Gemini CLI v0.29.5 (gemini-3.1-pro-preview), Claude Code (Opus 4.6)

**설계 문서:** `docs/plans/2026-02-25-v3.3-codex-gemini-design.md`

---

## Phase 1: Codex CLI 설정 (5 tasks)

### Task 1: Codex instructions.md 생성

**Files:**
- Create: `~/.codex/instructions.md`

**Step 1: 파일 생성**

`~/.codex/instructions.md` 내용 — 설계 문서 3.1.1절 전문 그대로 작성.

**Step 2: 검증**

```bash
codex exec --ephemeral --dangerously-bypass-approvals-and-sandbox "너의 system instructions 중에 '_meta 블록'이라는 단어가 있으면 YES, 없으면 NO로만 답해" 2>&1
```
Expected: YES 포함

**Step 3: 커밋**

```bash
cd /c/dev && git add -f ~/.codex/instructions.md
```
> Note: ~/.codex/는 dev-vault 밖이므로 별도 관리. 커밋 불필요 — 로컬 설정.

---

### Task 2: Codex config.toml 프로필 추가

**Files:**
- Modify: `~/.codex/config.toml`

**Step 1: 기존 config 읽기 후 프로필 추가**

기존 내용 유지하면서 `[profiles.extract]`와 `[profiles.verify]` 추가.

```toml
# 구조화 추출 (신규) — 컨텍스트 오프로딩용
[profiles.extract]
model = "gpt-5.3-codex"
model_reasoning_effort = "high"
sandbox_permissions = ["disk-full-read-access"]

# completeness 체크 (신규) — 빠른 검증용
[profiles.verify]
model = "gpt-5.3-codex"
model_reasoning_effort = "low"
sandbox_permissions = ["disk-full-read-access"]
```

**Step 2: 검증**

```bash
codex exec -p extract --ephemeral --dangerously-bypass-approvals-and-sandbox "현재 reasoning effort 설정이 뭔지 한 단어로만 답해" 2>&1
```
Expected: "high" 포함

```bash
codex exec -p verify --ephemeral --dangerously-bypass-approvals-and-sandbox "현재 reasoning effort 설정이 뭔지 한 단어로만 답해" 2>&1
```
Expected: "low" 포함

---

### Task 3: Codex prompts 디렉토리 + extract-git-history.md

**Files:**
- Create: `~/.codex/prompts/extract-git-history.md`

**Step 1: 디렉토리 생성 + 파일 작성**

```bash
mkdir -p ~/.codex/prompts
```

설계 문서 부록 A.1 전문 그대로 작성.

**Step 2: 검증**

```bash
ls ~/.codex/prompts/extract-git-history.md
```
Expected: 파일 존재

---

### Task 4: Codex prompts — review-diff.md

**Files:**
- Create: `~/.codex/prompts/review-diff.md`

**Step 1: 파일 작성**

설계 문서 부록 A.2 전문 그대로 작성.

**Step 2: 검증**

```bash
ls ~/.codex/prompts/review-diff.md
```
Expected: 파일 존재

---

### Task 5: Codex prompts — content-qa.md

**Files:**
- Create: `~/.codex/prompts/content-qa.md`

**Step 1: 파일 작성**

설계 문서 부록 A.3 전문 그대로 작성.

**Step 2: 검증**

```bash
ls ~/.codex/prompts/content-qa.md
```
Expected: 파일 존재

**Step 3: Phase 1 완료 확인**

```bash
echo "=== Codex 설정 확인 ===" && \
ls ~/.codex/instructions.md && \
ls ~/.codex/config.toml && \
ls ~/.codex/prompts/*.md
```
Expected: 5개 파일 모두 존재

---

## Phase 2: Gemini CLI 설정 (5 tasks)

### Task 6: Gemini GEMINI.md 생성

**Files:**
- Create: `~/.gemini/GEMINI.md`

**Step 1: 파일 작성**

설계 문서 3.1.2절 GEMINI.md 전문 그대로 작성.

**Step 2: 검증**

```bash
gemini -m gemini-3.1-pro-preview -p "너의 system instructions에 '_meta 블록'이라는 단어가 있으면 YES, 없으면 NO로만 답해" --yolo 2>/dev/null
```
Expected: YES 포함

---

### Task 7: Gemini skill — system-scanner

**Files:**
- Create: `~/.gemini/skills/system-scanner/SKILL.md`

**Step 1: 디렉토리 생성 + 파일 작성**

```bash
mkdir -p ~/.gemini/skills/system-scanner
```

설계 문서 3.1.2절 system-scanner 내용을 SKILL.md 형식(YAML frontmatter + 본문)으로 작성:

```markdown
---
name: system-scanner
description: Claude Code orchestration 시스템의 에이전트/스킬/훅 메타데이터를 구조화 추출. "시스템 스캔", "에이전트 목록", "인벤토리", "시스템 파악" 키워드에 트리거.
---

# System Scanner

## 절차 (순서대로, 건너뛰기 금지)

1. `~/.claude/agents/*.md` 파일 목록 확인
2. 각 파일에서 YAML frontmatter의 name, description, model, tools 추출
3. `~/.claude/skills/` 하위 디렉토리에서 각 SKILL.md의 name, description 추출
4. `~/.claude/settings.json`에서 hooks 섹션 추출
5. JSON으로 출력 (_meta 포함)

## 출력 스키마 (이 형식을 정확히 따를 것)

{
  "agents": [{"name":"","description":"","model":"","tools":[]}],
  "skills": [{"name":"","description":""}],
  "hooks": [{"event":"","command":""}],
  "_meta": {
    "files_scanned": 0,
    "fields_extracted": 0,
    "skipped": [],
    "model": "gemini-3.1-pro-preview",
    "completeness": "COMPLETE 또는 INCOMPLETE"
  }
}

## 금지사항
- 위 스키마 외 필드 추가 금지
- 해석/분석/추천 금지
- 파일 수정 금지
- 누락 필드는 null (추측 금지)
```

**Step 2: 검증**

```bash
gemini skills list 2>&1
```
Expected: "system-scanner [Enabled]" 표시

---

### Task 8: Gemini skill — project-scanner

**Files:**
- Create: `~/.gemini/skills/project-scanner/SKILL.md`

**Step 1: 디렉토리 생성 + 파일 작성**

```bash
mkdir -p ~/.gemini/skills/project-scanner
```

설계 문서 3.1.2절 project-scanner 내용을 SKILL.md 형식으로 작성. 절차:
1. 프로젝트 루트에서 파일 트리 생성 (node_modules, .git, dist, build 제외)
2. 각 TS/TSX/JS 파일에서 export 목록 추출
3. TypeScript interface/type 정의 추출
4. import 의존성 그래프 생성
5. 라우트 구조 추출 (Next.js app/ 또는 pages/)
6. JSON + _meta 출력

**Step 2: 검증**

```bash
gemini skills list 2>&1
```
Expected: "project-scanner [Enabled]" 표시

---

### Task 9: Gemini skill — state-scanner

**Files:**
- Create: `~/.gemini/skills/state-scanner/SKILL.md`

**Step 1: 디렉토리 생성 + 파일 작성**

```bash
mkdir -p ~/.gemini/skills/state-scanner
```

설계 문서 3.1.2절 state-scanner 내용을 SKILL.md 형식으로 작성. 절차:
1. 지정된 프로젝트 디렉토리들의 STATE.md 읽기
2. 각 프로젝트별 {name, status, in_progress, next} 추출
3. JSON + _meta 출력

프로젝트 경로 하드코딩:
- /c/dev/01_projects/01_orchestration/STATE.md
- /c/dev/01_projects/02_portfolio/STATE.md
- /c/dev/01_projects/03_tech-review/STATE.md
- /c/dev/01_projects/04_monet-lab/STATE.md

**Step 2: 검증**

```bash
gemini skills list 2>&1
```
Expected: "state-scanner [Enabled]" 표시

---

### Task 10: Gemini skill — news-verifier

**Files:**
- Create: `~/.gemini/skills/news-verifier/SKILL.md`

**Step 1: 디렉토리 생성 + 파일 작성**

```bash
mkdir -p ~/.gemini/skills/news-verifier
```

설계 문서 3.1.2절 news-verifier 내용을 SKILL.md 형식으로 작성. 절차:
1. 입력 마크다운에서 뉴스 항목 파싱 (## 섹션별)
2. 각 항목의 핵심 주장을 Google 검색으로 교차 확인
3. freshness, coverage gap 체크
4. JSON + _meta 출력

**Step 2: 검증**

```bash
gemini skills list 2>&1
```
Expected: 4개 스킬 모두 [Enabled] 표시

**Step 3: Phase 2 완료 — 통합 검증**

```bash
echo "=== Gemini 설정 확인 ===" && \
ls ~/.gemini/GEMINI.md && \
gemini skills list 2>&1
```
Expected: GEMINI.md 존재 + 4개 스킬 Enabled

---

## Phase 3: Claude Code 에이전트 수정 (3 tasks)

### Task 11: gemini-analyzer 에이전트 재작성

**Files:**
- Modify: `~/.claude/agents/gemini-analyzer.md`

**Step 1: 기존 파일 읽기**

현재 내용 확인 후 v3.3 벌크 추출기로 전면 재작성.

**Step 2: 재작성**

핵심 변경:
- description: "전체 코드베이스 분석" → "대량 파일 구조화 추출 + 웹 검색. 컨텍스트 오프로딩의 1순위."
- 모든 호출 예시에 `-m gemini-3.1-pro-preview` + `--output-format json` + `--yolo` 포함
- 4가지 스킬 활용 방법 명시 (system-scanner, project-scanner, state-scanner, news-verifier)
- _meta 블록 검증 절차 포함
- 기존 "교차 검증 파이프라인" 다이어그램 제거 → v3.3 추출 파이프라인으로 대체

**Step 3: 검증**

에이전트 frontmatter의 name, description, tools, model이 올바른지 확인.

---

### Task 12: codex-reviewer 에이전트 재작성

**Files:**
- Modify: `~/.claude/agents/codex-reviewer.md`

**Step 1: 기존 파일 읽기 후 재작성**

핵심 변경:
- description: "8개 검증 관점 독립 분석" → "정밀 검증기. diff 리뷰 + 포맷 QA + git 히스토리 추출. Codex 5시간 롤링 제한 인지."
- 8관점 검사 → instructions.md로 이동 완료 (에이전트에서 중복 제거)
- 3가지 프로필 활용: extract, verify, review
- 프롬프트 템플릿 활용 방법 명시
- 자원 관리 규칙 명시 (세션당 3~5회)

**Step 2: 검증**

에이전트 frontmatter 확인.

---

### Task 13: ai-synthesizer 에이전트 재작성

**Files:**
- Modify: `~/.claude/agents/ai-synthesizer.md`

**Step 1: 기존 파일 읽기 후 재작성**

핵심 변경:
- description: "교차 검증 합성" → "adversarial verifier. 모든 외부 CLI 출력의 verify barrier. completeness check + 반박 검증."
- 분류 매트릭스 유지하되 verify barrier 3단계 추가:
  1. 구조 검증 (_meta 파싱)
  2. 스팟체크 (2~3개 항목 원본 대조)
  3. 반박 검증 (고위험 시)
- tools에 Read 추가 (스팟체크용)

**Step 2: 검증**

에이전트 frontmatter 확인.

**Step 3: Phase 3 커밋**

```bash
cd /c/dev && git add ~/.claude/agents/gemini-analyzer.md ~/.claude/agents/codex-reviewer.md ~/.claude/agents/ai-synthesizer.md
git commit -m "[orchestration] v3.3 에이전트 3개 재작성: gemini-analyzer→벌크추출, codex-reviewer→정밀검증, ai-synthesizer→adversarial verify"
```

---

## Phase 4: Claude Code 스킬 생성 (3 tasks)

### Task 14: context-scan 스킬 생성

**Files:**
- Create: `~/.claude/skills/context-scan/SKILL.md`

**Step 1: 디렉토리 생성 + 파일 작성**

```bash
mkdir -p ~/.claude/skills/context-scan
```

설계 문서 부록 B.1 전문을 SKILL.md 형식으로 작성. 핵심:
- /context-scan, /context-scan system, /context-scan [프로젝트명], /context-scan state, /context-scan git
- CLI 사전 확인 (which gemini/codex)
- 5가지 추출 실행 (Gemini 스킬 호출 + Codex git)
- verify barrier 필수
- 출력 포맷

**Step 2: 검증**

스킬이 Claude Code에서 인식되는지 확인 (settings.json에 등록 불필요 — 디렉토리 기반 자동 발견).

---

### Task 15: tr-verify 스킬 생성

**Files:**
- Create: `~/.claude/skills/tr-verify/SKILL.md`

**Step 1: 디렉토리 생성 + 파일 작성**

```bash
mkdir -p ~/.claude/skills/tr-verify
```

설계 문서 부록 B.2 전문을 SKILL.md 형식으로 작성. 핵심:
- /tr-verify, /tr-verify [YYYY-MM-DD]
- git pull → Gemini news-verifier (병렬) + Codex content-qa (병렬) → verify barrier → 종합 보고
- 자동 수정 옵션

---

### Task 16: cross-review 스킬 생성

**Files:**
- Create: `~/.claude/skills/cross-review/SKILL.md`

**Step 1: 디렉토리 생성 + 파일 작성**

```bash
mkdir -p ~/.claude/skills/cross-review
```

설계 문서 부록 B.3 전문을 SKILL.md 형식으로 작성. 핵심:
- /cross-review, /cross-review [경로], /cross-review --security
- diff 규모 확인 → Codex review --uncommitted → Claude 교차 → 보고
- 자원 관리 (세션당 1~2회)

**Step 2: Phase 4 커밋**

```bash
cd /c/dev && git add ~/.claude/skills/context-scan/ ~/.claude/skills/tr-verify/ ~/.claude/skills/cross-review/
git commit -m "[orchestration] v3.3 스킬 3개 신규: /context-scan, /tr-verify, /cross-review"
```

---

## Phase 5: Living Docs 업데이트 (4 tasks)

### Task 17: STATE.md 업데이트

**Files:**
- Modify: `/c/dev/01_projects/01_orchestration/STATE.md`

**Step 1: 읽기 후 수정**

변경 사항:
- 시스템 버전: v3.2 → v3.3
- 마지막 갱신 날짜 업데이트
- orchestration 진행 중 섹션: v3.3 내용 추가
- 인벤토리 에이전트: 역할 설명 업데이트 (gemini-analyzer, codex-reviewer, ai-synthesizer)
- 인벤토리 스킬: /context-scan, /tr-verify, /cross-review 추가 (11개 → 14개)
- 외부 CLI 설정 섹션 신규 추가: Codex 설정 (instructions.md, 3 profiles, 3 prompts) + Gemini 설정 (GEMINI.md, 4 skills)

---

### Task 18: CLAUDE.md 체인 업데이트

**Files:**
- Modify: `/c/dev/CLAUDE.md`

**Step 1: 읽기 후 분석 체인 수정**

변경 사항:
- 분석 체인을 추출/검증 체인으로 변경:
  ```
  ### 추출/검증 체인 (v3.3)
  Gemini 추출(벌크) + Codex 추출(정밀) → Claude verify barrier(3단계) → 사용
  - Gemini: -m gemini-3.1-pro-preview --output-format json --yolo
  - Codex: -p [extract|verify|review] --ephemeral --dangerously-bypass-approvals-and-sandbox
  - 외부 CLI 출력은 반드시 _meta 블록 검증 후 사용
  ```
- 스킬 목록에 /context-scan, /tr-verify, /cross-review 추가

---

### Task 19: CHANGELOG.md 업데이트

**Files:**
- Modify: `/c/dev/01_projects/01_orchestration/docs/CHANGELOG.md`

**Step 1: 읽기 후 v3.3 엔트리 추가**

```markdown
## v3.3 — Codex/Gemini CLI 통합 강화 (2026-02-25)

### 추가
- Codex CLI 설정: instructions.md(글로벌 페르소나), config.toml 프로필 3종(review/extract/verify), prompts 3종
- Gemini CLI 설정: GEMINI.md(글로벌 페르소나), 커스텀 스킬 4종(system-scanner/project-scanner/state-scanner/news-verifier)
- Claude Code 스킬 3종: /context-scan(컨텍스트 오프로딩), /tr-verify(tech-review QA), /cross-review(병렬 코드 리뷰)
- Verify Barrier: 모든 외부 CLI 출력에 3단계 검증(구조→스팟체크→반박)
- _meta 블록: 외부 CLI JSON 출력에 검증용 메타데이터 강제

### 변경
- gemini-analyzer: 코드베이스 분석 → 벌크 추출기(컨텍스트 오프로딩 + 웹 검색)
- codex-reviewer: 8관점 설계 결함 → 정밀 검증기(diff 리뷰 + 포맷 QA + git 추출)
- ai-synthesizer: 교차 검증 합성 → adversarial verifier(completeness + 반박 검증)
- 분석 체인 → 추출/검증 체인 재정의

### 설계 원칙
- Claude = 유일한 설계/결정권자. 외부 CLI = 사실 확인과 추출만.
- 해석이 아니라 추출. JSON 구조화 출력으로 신뢰성 확보.
- Gemini = 벌크(넉넉, 1M). Codex = 정밀(귀한, 5시간 롤링).
- 세션당 컨텍스트 절약 추정: ~170K 토큰 (88%)
```

---

### Task 20: MEMORY.md 업데이트

**Files:**
- Modify: `/c/Users/pauls/.claude/projects/C--dev/memory/MEMORY.md`

**Step 1: 시스템 버전 업데이트**

System Version 섹션 수정:
```
- v3.3 (2026-02-25): Codex/Gemini CLI 통합 강화 + 컨텍스트 오프로딩 + verify barrier
```

Codex CLI 연동 섹션에 프로필 정보 추가.

**Step 2: Phase 5 커밋**

```bash
cd /c/dev && git add 01_projects/01_orchestration/STATE.md CLAUDE.md 01_projects/01_orchestration/docs/CHANGELOG.md
git commit -m "[orchestration] v3.3 Living Docs 업데이트: STATE/CLAUDE/CHANGELOG"
```

---

## Phase 6: 통합 검증 (3 tasks)

### Task 21: Gemini 스킬 실전 테스트 — system-scanner

**Step 1: system-scanner 실행**

```bash
gemini -m gemini-3.1-pro-preview -p "system-scanner 스킬로 시스템 스캔해" --output-format json --yolo 2>/dev/null
```

**Step 2: 결과 검증**

- JSON 파싱 성공 여부
- _meta.files_scanned가 0이 아닌지
- agents 배열에 항목이 있는지
- 임의 에이전트 1개를 원본 파일과 대조

---

### Task 22: Codex 프로필 실전 테스트 — extract

**Step 1: git 히스토리 추출 실행**

```bash
codex exec -p extract --ephemeral --dangerously-bypass-approvals-and-sandbox -C /c/dev "최근 3일 git log를 JSON으로 구조화해. commits 배열에 date, message, files_changed 포함. _meta 블록 필수." 2>&1
```

**Step 2: 결과 검증**

- JSON 파싱 성공 여부
- commits 배열에 실제 커밋이 포함됐는지
- `git log --since="3 days ago" --oneline`과 대조

---

### Task 23: 전체 시스템 정합성 확인

**Step 1: 체크리스트**

```
[ ] ~/.codex/instructions.md 존재 + Codex가 읽음
[ ] ~/.codex/config.toml에 extract/verify 프로필 존재
[ ] ~/.codex/prompts/ 3개 파일 존재
[ ] ~/.gemini/GEMINI.md 존재 + Gemini가 읽음
[ ] ~/.gemini/skills/ 4개 스킬 Enabled
[ ] ~/.claude/agents/ 3개 에이전트 v3.3 내용
[ ] ~/.claude/skills/ 3개 신규 스킬 존재
[ ] STATE.md v3.3 반영
[ ] CLAUDE.md 추출/검증 체인 반영
[ ] CHANGELOG.md v3.3 엔트리
[ ] MEMORY.md 버전 업데이트
```

**Step 2: 최종 커밋 + 푸시**

```bash
cd /c/dev && git add -A && git status
```

미커밋 항목 있으면 정리 후:
```bash
git commit -m "[orchestration] v3.3 완료: Codex/Gemini CLI 통합 강화"
git push
```
